// Creating an Object in js

const student = {
  name: "John",
  age: 17,
}

// Read objects in js

console.log(student.name, student.age)

// creating an array in js

var x = []
var y = [1, 2, 3, 4, 5]

// read array elements in js

for (var i = 0; i < y.length; i++) {
  console.log(i)
}

// get typeof arguments in js

function func(x) {
  console.log(typeof x, arguments.length)
}
func(1)

size of localStorage

upto 10mb

semantic elemeents

